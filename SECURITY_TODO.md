# VestHub — Security & Hardening Follow-ups

این سند کارهای امنیتی/زیرساختی باقی‌مانده را پس از اصلاحات کریتیکال (شاخه‌ی `fix/critical-security-issues`) فهرست می‌کند.
تاریخ مرور: ۲۰۲۶-۰۶-۳۰.

---

## ✅ انجام‌شده (در همین مرور)
- **C1** حذف روت فعال‌سازی رایگان سرمایه‌گذاری (`POST /payment/process/<id>`).
- **C2** اجباری‌کردن ۲FA در فرایند لاگین.
- **C3** انتخاب پیکربندی از `APP_ENV` با پیش‌فرض `production` + سخت‌سازی کوکی‌ها (`Secure`, `HttpOnly`, `SameSite=Lax`).
- **C4** کنترل دسترسی روی `admin.dashboard` و `api_admin_stats`.
- **C5** محدودیت تلاش/انقضا روی کد برداشت و کد تأیید ایمیل.
- **C7** حذف فراخوانی HTTP خارجی مسدودکننده در `get_locale`.
- **C8** کش‌کردن `/api/market-data`.
- **C9** حذف کلید fallback قابل‌حدس برای `SECRET_KEY`.
- اصلاحات جزئی: تراکنش یتیم در لغو سرمایه‌گذاری، چک وضعیت در approve/reject برداشت، ایمن‌سازی `user.role` خالی.

---

## 🔴 اولویت بالا — نیازمند اقدام

### 1. مهاجرت از SQLite به PostgreSQL (C6)
- **چرا:** روی SQLite `with_for_update()` بی‌اثر است؛ دفترکل مالی با نوشتن همزمان مستعد race condition و خطای `database is locked` است.
- **اقدام:** راه‌اندازی Postgres، تنظیم `DATABASE_URL`، انتقال داده‌ها، تست قفل ردیفی در مسیر برداشت و تسک سود.
- **نکته:** تا قبل از مهاجرت، روی صحت `with_for_update` برای جلوگیری از برداشت دوگانه حساب نکنید.

### 2. تنظیم متغیرهای محیطی production
- `APP_ENV=production` (یا حذف override به development) روی سرور زنده.
- `SECRET_KEY` پایدار و قوی (۶۴ کاراکتر hex، مثلاً `python -c "import secrets;print(secrets.token_hex(32))"`).
  بدون کلید ثابت در env، با هر ری‌استارت/worker سشن‌ها باطل می‌شوند.

### 3. درگاه پرداخت واقعی (جایگزین C1)
- روت حذف‌شده هیچ تأیید پرداختی نداشت. اگر پرداخت آنلاین لازم است، یک درگاه معتبر (با وبهوک/تأیید سمت‌سرور) یکپارچه شود؛ فعال‌سازی سرمایه‌گذاری فقط پس از تأیید قطعی پرداخت.

---

## 🟠 اولویت متوسط

### 4. Rate limiting سراسری
- افزودن `Flask-Limiter` روی `login`, `signup`, `forgot-password`, `verify-*`, `withdrawal`.
- محدودیت‌های مبتنی بر IP (پشت Cloudflare از هدر واقعی IP استفاده شود).

### 5. فعال‌سازی Flask-Talisman
- پکیج `flask-talisman` در `requirements.txt` هست ولی استفاده نشده. برای هدرهای امنیتی (CSP، HSTS، X-Frame-Options) فعال شود.

### 6. اعتبارسنجی ورودی‌ها
- `create_investment`: اعتبارسنجی `plan_id` (وجود پلن، فعال‌بودن، حداقل/حداکثر مبلغ).
- بررسی سقف/کف مبلغ سرمایه‌گذاری و برداشت مطابق قوانین کسب‌وکار.

### 7. صحت ایمیل‌ها و احراز
- در `submit_proof` و سایر جریان‌ها، آدرس مقصد ایمیل ادمین (`info@vesthub.org`) به‌جای hardcode از تنظیمات خوانده شود.
- بررسی محدودیت ارسال مجدد کد تأیید (resend throttling).

---

## 🟡 اولویت پایین / بهبود

### 8. کش بازار (C8) — نسخه‌ی مقیاس‌پذیر
- کش فعلی درون‌حافظه‌ای و per-worker است. برای چند worker، استفاده از Redis یا یک job پس‌زمینه‌ی مشترک بهتر است.

### 9. لاگ‌گیری و پایش
- ارسال خطاهای ۵۰۰ به یک سیستم پایش (Sentry و …).
- اطمینان از عدم نشت اطلاعات حساس در لاگ‌ها/پیام‌های خطا (مثلاً `Authentication failed: {str(e)}` در social login).

### 10. سریال‌سازی numpy در `/api/market-data`
- مقادیر `round(numpy.float)` را به `float` پایتون تبدیل کنید تا از خطاهای احتمالی JSON جلوگیری شود.

### 11. تست‌ها
- افزودن تست‌های خودکار برای مسیرهای مالی (سرمایه‌گذاری، برداشت، تأیید پرداخت) و کنترل دسترسی ادمین.
